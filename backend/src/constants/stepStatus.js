export const STEP_STATUS = Object.freeze({
  PENDING: 'pending',
  COMPLETED: 'completed',
  SKIPPED: 'skipped',
});

export const STEP_STATUS_VALUES = Object.values(STEP_STATUS);

export default STEP_STATUS;
